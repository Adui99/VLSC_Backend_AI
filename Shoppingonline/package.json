{
  "name": "shoppingonline-app",
  "version": "1.0.0",
  "description": "MERN Stack Shopping Online - Lab 10 Production Deployment",
  "main": "server/index.js",
  "scripts": {
    "install:all": "npm install && npm --prefix client-customer install && npm --prefix client-admin install && npm --prefix server install",
    "build:customer": "npm --prefix client-customer install && npm --prefix client-customer run build",
    "build:admin": "npm --prefix client-admin install && npm --prefix client-admin run build",
    "build": "npm run build:customer && npm run build:admin && node scripts/prepare-production.js",
    "start": "node server/index.js"
  },
  "keywords": [
    "mern",
    "shopping-online",
    "ecommerce",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "Adui99",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.3.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.4",
    "nodemailer": "^9.0.3"
  }
}
